const classes = require('./classes')
const users = require('./users')
const sessions = require('./sessions')
const classStudents = require('./classes/students')

module.exports = {
  classes,
  users,
  sessions,
  classStudents,
}
